[build]
  publish = "dist"
  # Build the Vue app, then install function dependencies similar to your working repo
  command = "npm run build && cd netlify/functions && npm install"

[dev]
  port = 8888

[functions]
  directory = "netlify/functions"
